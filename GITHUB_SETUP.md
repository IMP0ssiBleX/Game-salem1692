# วิธีอัปโหลดเกมขึ้น GitHub และทำเว็บออนไลน์ฟรี (GitHub Pages)

นี่คือขั้นตอนการนำเกม **Salem 1692** ขึ้น GitHub เพื่อให้เพื่อนๆ เล่นออนไลน์ได้ครับ

## สิ่งที่ต้องมี
1. บัญชี **GitHub** (สมัครได้ที่ github.com)
2. ติดตั้ง **Git** ในเครื่อง (ดาวน์โหลดที่ git-scm.com)

## ขั้นตอนที่ 1: สร้าง Repository บน GitHub
1. ไปที่ [GitHub.com](https://github.com) แล้วล็อกอิน
2. กดปุ่ม **New** (สีเขียว) เพื่อสร้าง Repository ใหม่
3. ตั้งชื่อ Repository (เช่น `salem-1692`)
4. เลือกเป็น **Public** (สาธารณะ)
5. กด **Create repository**
6. *ไม่ต้องปิดหน้านั้น* ให้เปิดทิ้งไว้ก่อน

## ขั้นตอนที่ 2: อัปโหลดโค้ดจากเครื่องคุณ
เปิด **Terminal** หรือ **Command Prompt** ในโฟลเดอร์เกมนี้ แล้วพิมพ์คำสั่งต่อไปนี้ (ทีละบรรทัด):

```bash
# 1. เริ่มต้นระบบ Git
git init

# 2. เพิ่มไฟล์ทั้งหมดเข้า Git
git add .

# 3. บันทึกไฟล์ (Commit)
git commit -m "First commit - Salem 1692 Game"

# 4. เปลี่ยนชื่อ branch หลักเป็น main
git branch -M main

# 5. เชื่อมต่อกับ GitHub (แก้ URL ด้านล่างเป็นของคุณเอง)
# ดู URL ได้จากหน้าที่เปิดทิ้งไว้ในขั้นตอนที่ 1
git remote add origin https://github.com/USERNAME/salem-1692.git

# 6. อัปโหลดขึ้น GitHub
git push -u origin main
```

## ขั้นตอนที่ 3: เปิดให้เล่นออนไลน์ (GitHub Pages)
1. กลับไปที่หน้า Repository ของคุณบน GitHub
2. ไปที่เมนู **Settings** (แถบด้านบน)
3. เมนูด้านซ้าย เลือก **Pages**
4. ตรงหัวข้อ **Build and deployment** > **Branch**
   - เลือก `main`
   - เลือกโฟลเดอร์ `/ (root)`
   - กด **Save**
5. รอสักครู่ (ประมาณ 1-2 นาที) รีเฟรชหน้าเดิม
6. GitHub จะโชว์ลิงก์เว็บด้านบน (เช่น `https://yourname.github.io/salem-1692/`)

**เสร็จสิ้น!** ส่งลิงก์นั้นให้เพื่อนเล่นได้เลยครับ 🎮
